package ForceExtensions

public function force.pingUnitsForForce(vec2 pos, real duration, integer style, color c)
    PingMinimapForForceEx(this, pos.x, pos.y, duration, style, c.red.toReal(), c.green.toReal(), c.blue.toReal())

library v2103e requires ChangeLog {
  function onInit() {
    ChangeLog changes = ChangeLog.create(2, 103, 'e');
    changes.add("Fixed bee hive having two charges by default");
    changes.add("Fixed -or mode always being on");
    changes.add("Beast Master no longer needs to last hit");
    changes.add("Blowgun can now be loaded in inventory (thistles only)");
  }
}
{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "itt.legacycompile",
            "type": "shell",
            "command": "bash -c 'ruby ./bin/compile'",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "itt.legacycompilepro",
            "type": "shell",
            "command": "bash -c 'ruby ./bin/compile env:pro'",
            "group": "none"
        }
    ]
}

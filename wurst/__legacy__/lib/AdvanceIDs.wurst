package AdvanceIDs

import ObjectIdGenerator
import ObjectIds

@compiletime
function advanceAbilityIDs()
    for i = 0 to 1000
        ABIL_ID_GEN.next()
    print("Ability IDs were advanced to: " + toRawCode(ABIL_ID_GEN.next()))

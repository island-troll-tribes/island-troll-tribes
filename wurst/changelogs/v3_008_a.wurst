package v3_008_a

import ChangeLog

init
    new ChangeLog(3, 8, 'a')
        ..add("Added mode for configuring when the central gate is opened.")
        ..add("Changed modes that set a timer, such as grace period and forced duel, to use seconds instead of minutes.")
        ..add("Changed the UI to display effects on units more prominently.")
        ..add("Disabled Reforged graphics.")
        ..add("Fixed bug where players would desync depending on their setting for hints.")

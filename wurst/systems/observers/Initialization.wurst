package Initialization

init
    for i = 0 to bj_MAX_PLAYERS - 1
        if IsPlayerObserver(players[i])
            isobserver[i] = true
            obs_present = true
        else
            isobserver[i] = false

package v2_103_e
import ChangeLog

init
    new ChangeLog(2, 103, 'e')
    ..add("Fixed bee hive having two charges by default")
    ..add("Fixed -or mode always being on")
    ..add("Beast Master no longer needs to last hit")
    ..add("Blowgun can now be loaded in inventory (thistles only)")

package DropItems
import RegisterEvents

public constant SPELL_DROP_ITEMS = 'A06P'

init
    registerSpellEffectEvent(SPELL_DROP_ITEMS) ->
        let caster = GetSpellAbilityUnit()
        let size = caster.inventorySize()
        for slot = 0 to size - 1
            caster.removeItemFromSlot(slot)

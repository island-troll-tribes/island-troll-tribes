library v295b requires ChangeLog {
  function onInit() {
    ChangeLog changes = ChangeLog.create(2, 95, 'b');
    changes.add("You will now spawn with your team in the duel arena (but location is still random)");
    changes.add("Fix people spawning on wall of duel arena in forced duel");
  }
}
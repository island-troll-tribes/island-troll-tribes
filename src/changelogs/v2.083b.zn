
library v0283b requires ChangeLog {
	function onInit() {
		ChangeLog changes = ChangeLog.create( 2, 83, 'b' );
		changes.push("Fixed being able to carry both battle shield and battle axe, can now only carry one or the other.");
	}
}
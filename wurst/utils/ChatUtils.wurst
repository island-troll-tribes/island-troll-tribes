package ChatUtils

public function string.isConfirmation() returns boolean
    let lower = this.toLowerCase()
    return lower == "no" or lower == "off" or lower == "false" or lower == "0"

public function bool.toOnOff() returns string
    return this ? "on" : "off"

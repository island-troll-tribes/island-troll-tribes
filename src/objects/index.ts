/**
 * Object Definitions Index
 *
 * Import all compiletime object definitions. These files use
 * war3-transformer's compiletime() to create custom WC3 objects
 * (units, items, abilities) in the Object Editor at build time.
 *
 * The FourCC IDs used here match those in data/UnitIds.ts,
 * data/ItemIds.ts, and data/AbilityIds.ts.
 */

import "./TrollObjects";
import "./AnimalObjects";
import "./BuildingObjects";
import "./ItemObjects";
import "./AbilityObjects";
import "./BuffObjects";

package v3_009_c

import ChangeLog

init
    new ChangeLog(3, 9, 'c')
        ..add("Fixed bug where Depression Orb spawned a hero.")
        ..add("Fixed bug where Jungle Tyrant wasn't a hero.")
        ..add("Fixed bug where Spiked Trap spawned a hero.")
        ..add("Fixed bug where Warrior wasn't a hero.")

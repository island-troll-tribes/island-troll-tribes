

library v288g requires ChangeLog {
  function onInit() {
    ChangeLog changes = ChangeLog.create( 2, 88, 'g' );
    changes.push("Fix items from spawning when you sell an item");
    changes.push("Fix medallion of courage not working");
    changes.push("Fix timer spam (hopefully)");
  }
}

/**
 * Beta text is still in income box
 * Disease still says it heals 30 health
 * Might be leaking timers...
 * Acid bomb is fucked (lags map to shit)
 * Supposedly magic d/c'd Srup
 */
library v3000m requires ChangeLog {
  function onInit() {
    ChangeLog changes = ChangeLog.create(3, 0, 'm');
    changes.add("Fixed thief's bush waterfalls which were bugging units");
    changes.add("Fixed bug which was causing spirit wards to reset and cancel revives");
  }
}
